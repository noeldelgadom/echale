.col-md-6.col-md-offset-3
  #teams.transitions-enabled
    - @teams.each do |team|
      .box.panel.panel-default
        = link_to (image_tag team.image.url), team
        %h2= link_to team.name, team
